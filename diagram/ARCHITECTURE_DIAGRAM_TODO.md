# Architecture Diagram — MISSING

The assignment (Step 2) requires a **hand-annotated architecture diagram**:
take the microGPT architecture diagram/visualization and hand-annotate it
(pen-and-paper scan/photo, or digital handwriting on a tablet/stylus),
explicitly mapping each conceptual block — embeddings, self-attention, MLP,
LM head, train loop, inference generation — to the exact corresponding lines
of code in the raw microGPT gist.

**This image does not exist anywhere in the repo yet.**

`diagram/blueprint_notes.md` (this folder) contains only *typed prose notes*
summarizing how microGPT works (Step 1: "Understand the blueprint") — it is
**not** a substitute for the required hand-annotated diagram image.

## Action needed

Whoever owns Task 1 (architecture diagram) should drop the scanned/exported
image file here as:

```
diagram/architecture_diagram.png
```

(or `.jpg` / `.pdf` — update the extension in `README.md` and
`report/report.md` to match once added; both currently link to
this exact path and both currently flag it as pending).

Once the file is in place, this `ARCHITECTURE_DIAGRAM_TODO.md` file can be
deleted.
